---
"@1001-digital/dweb-fetch": minor
---

Make `@helia/verified-fetch` an optional peer dependency so gateway-mode consumers do not install Helia/libp2p, while verified mode can still use it when installed.
